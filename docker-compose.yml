version: '3.8'

services:
  # PHP Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streampilot-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
      - ./storage/app/epg:/var/www/html/storage/app/epg
    networks:
      - streampilot
    depends_on:
      - mysql
      - redis

  # Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: streampilot-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - streampilot
    depends_on:
      - app

  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: streampilot-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: streampilot
      MYSQL_USER: streampilot
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - streampilot

  # Redis Cache
  redis:
    image: redis:alpine
    container_name: streampilot-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - streampilot

  # Queue Worker
  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streampilot-queue
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
    volumes:
      - .:/var/www/html
    networks:
      - streampilot
    depends_on:
      - app
      - mysql
      - redis

  # Scheduler
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streampilot-scheduler
    restart: unless-stopped
    working_dir: /var/www/html
    command: sh -c "while true; do php artisan schedule:run --verbose --no-interaction & sleep 60; done"
    volumes:
      - .:/var/www/html
    networks:
      - streampilot
    depends_on:
      - app
      - mysql
      - redis

networks:
  streampilot:
    driver: bridge

volumes:
  mysql_data:
  redis_data:
